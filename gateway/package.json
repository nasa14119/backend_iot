{
  "name": "gateway",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch index.ts",
    "build": "NODE_ENV=production bun build --target bun ./index.ts --outdir dist"
  },
  "dependencies": {
    "@formkit/tempo": "^0.1.2",
    "drizzle-orm": "^0.44.6",
    "drizzle-seed": "^0.3.1",
    "elysia": "latest",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "bun-types": "latest",
    "drizzle-kit": "^0.31.5"
  },
  "module": "src/index.js"
}
